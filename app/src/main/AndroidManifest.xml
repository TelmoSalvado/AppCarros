<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.ipg.appcarros">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCarros">
        <activity android:name=".InserirOficina"></activity>
        <activity
            android:name=".Oficina"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".InserirCarroActivity"
            android:parentActivityName=".CarroActivity" />
        <activity android:name=".CarroActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".PerfilActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".HomeActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".RegistoActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>